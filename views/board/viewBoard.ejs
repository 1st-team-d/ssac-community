<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>새싹 스터디 게시판</title>
    <!-- CDN -->
    <%- include('../cdn/cdn.ejs') %>
    <!-- 게시판 목록 조회, 특정 게시글 조회 -->
    <link rel="stylesheet" href="/static/css/boardCommon.css" />
    <link rel="stylesheet" href="/static/css/viewBoard.css" />
    <script defer src="/static/js/viewBoard.js"></script>
  </head>
  <body>
    <div class="board_wrap container">
      <div class="board_title">
        <strong>새싹 스터디 게시판🌱</strong>
        <p>새싹 스터디 게시판입니다. 자유롭게 작성해주세요~~</p>
      </div>
      <div class="board_view_wrap">
        <div class="board_view">
          <div class="title"><%= data.postTitle %></div>
          <div class="info">
            <dl>
              <dt>번호</dt>
              <dd><%= data.postId %></dd>
            </dl>
            <dl>
              <dt>글쓴이</dt>
              <dd><%= data.userName %></dd>
            </dl>
            <dl>
              <dt>작성일</dt>
              <dd><%= data.postDate %></dd>
            </dl>
            <dl>
              <dt>조회</dt>
              <dd><%= data.postCount %></dd>
            </dl>
          </div>
          <div class="cont">
            <!-- 게시물 내용을 출력할 때는 줄바꿈 이런걸 했을 수도 있어서 이스케이프 처리하지 않고 출력 -->
            <%- data.postContent.replace(/\n/g, '<br />') %>
          </div>
        </div>
        <div id="postFile" class="fs-3 my-3">
          <span class="badge text-bg-success me-3">첨부파일</span>
          <a href="<%= data.postFilePath %>" target="_blank"
            ><%= data.postFileName %></a
          >
        </div>
      </div>
      <!-- JavaScript 코드와 스타일이 적용된 댓글 목록 -->
      <div class="comment_section">
        <h2>댓글 작성</h2>
        <div class="comment_form">
          <div class="comment_content mb-4">
            <span id="commentWriter" class="fs-3 fw-bold p-3"
              ><%= loginUserData.userName %></span
            >
            <textarea
              class="px-3"
              id="comment_content"
              name="comment_content"
            ></textarea>
          </div>
          <div id="btnArea" class="d-flex justify-content-end">
            <button
              class="btn fw-bold fs-4 p-3 text-light"
              type="submit"
              id="submit_comment"
              onclick="submitComment()"
            >
              댓글 작성
            </button>
          </div>
        </div>
        <div class="card-body comment_list"></div>
      </div>

      <div class="bt_wrap container d-flex justify-content-center">
        <button class="btn me-5 fs-2 px-4">목록</button>
        <button class="btn fs-2 px-4">수정</button>
      </div>
    </div>
  </body>
</html>
