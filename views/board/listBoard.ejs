<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>새싹 스터디 게시판</title>
    <link rel="stylesheet" href="/static/css/boardCommon.css" />
    <link rel="stylesheet" href="/static/css/boardSearch.css" />
  </head>
  <body>
    <div class="board_wrap">
      <div class="board_title">
        <strong>새싹 스터디 게시판🌱</strong>
        <p>새싹 스터디 게시판입니다. 자유롭게 작성해주세요~~</p>
      </div>

      <div class="board_list_wrap">
        <!-- 게시물 -->
        <div class="board_list" id="boardList">
          <% let count = 1 %> <% data.forEach((board) => { %>
          <div>
            <div class="num"><%= count %></div>
            <div class="title">
              <a
                href="/board?boardSeq=<%= board.dataValues.boardSeq %>"
                class="view-link"
              >
                <%= board.dataValues.title %>
              </a>
            </div>
            <div class="date"><%= board.dataValues.createdAt %></div>
            <div class="count"><%= board.dataValues.count %></div>
          </div>
          <% count++ %> <% }) %>
        </div>
        <!-- 검색 창 -->
        <div class="search_box">
          <input type="text" placeholder="검색어를 입력하세요" />
          <button>검색</button>
        </div>
        <!-- 페이지 번호 -->
        <div class="board_page" id="pagination"></div>
        <div class="bt_wrap">
          <a href="postBoard.ejs" class="on">글 작성</a>
        </div>
      </div>
    </div>

    <!-- listBoard.js 파일을 연결 -->
    <script src="/static/js/listBoard.js"></script>
  </body>
</html>
